# display program title
# initialize a variable even_count to 0 to store the count of even numbers
# initialize an empty list even_numbers to store even numbers
# use for loop to iterate 10 times, once for each number
# if the number is even, increment even_count and add the number to even_numbers list
# display the count of even numbers and the list of even numbers